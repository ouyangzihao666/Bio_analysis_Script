#!/usr/bin/env python3
"""mdkit command-line entry point.

Run with the ``bioAna_gmx_user02`` conda environment active (or any Python
3.9+ with PyYAML installed).
"""

import os
import sys

# Script lives at <gromacs>/mdkit/mdkit; the package is <gromacs>/mdkit.
sys.path.insert(
    0, os.path.dirname(os.path.dirname(os.path.realpath(__file__)))
)

from mdkit.cli import main


if __name__ == "__main__":
    sys.exit(main())
